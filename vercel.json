{
  "installCommand": "npm install",
  "buildCommand": "npm run build:web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets|_expo|fonts).*)",
      "destination": "/index.html"
    }
  ]
}
